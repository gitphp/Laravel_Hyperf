# 业财一体化财务系统｜PHP8.3 + Laravel12 + MySQL8 模块+架构设计
> 业财一体化核心：**业务单据自动生成财务凭证**，业务（销售、采购、库存）和财务数据同源，杜绝业务、财务两套账；适合中小企业，可和上面CRM打通。
> 技术栈：Laravel12、PHP8.3、MySQL8、Redis、PhpSpreadsheet(Excel导入导出)、队列Job、事件监听、Laravel Permission(RBAC权限)。

## 一、整体业务模块划分
### 🔐 1、系统基础模块（公共底座）
1. 用户管理、部门、岗位
2. RBAC角色权限：菜单权限、按钮权限、**数据权限**（只能看本部门/全部财务数据）
3. 操作日志、登录日志；单据操作留痕
4. 系统字典：收支类型、结算方式、币种、税率、凭证字、往来性质
5. 参数配置：会计期间（自然月）、是否开启审核、凭证自动生成开关、小数精度配置

### 📚 2、基础档案模块（财务主数据，所有业务依赖）
> 业财一体化最重要的基础数据
1. **会计科目表**：资产、负债、权益、成本、损益；支持二级、多级科目；辅助核算（客户、供应商、部门、员工、项目）
2. 往来单位档案：客户、供应商（可对接CRM客户数据）
3. 员工档案、项目档案、仓库档案、商品档案（对接业务销售/采购）
4. 税率档案：增值税、普通税率
5. 结算账户：公司银行账户、微信、支付宝账户

### 🧾 3、采购业务模块（业务层）
> 业务单据，审核后自动生成会计凭证
1. 采购申请单
2. 采购订单
3. 采购入库单
4. 采购退货单
5. 采购付款单（付给供应商）
> 单据状态：待审核、已审核、已作废；审核触发事件，自动生成记账凭证

### 🛒 4、销售业务模块（可对接CRM合同/订单）
1. 销售订单
2. 销售出库单
3. 销售退货单
4. 销售收款单（向客户收钱）
> 来源可以来自CRM的合同、回款记录，接口同步过来，不需要重复录入

### 📦 5、库存存货模块（业务层）
1. 入库、出库、调拨、盘点单
2. 存货成本核算（加权平均）
3. 库存台账
> 出入库审核后生成存货相关财务凭证

### 💰 6、应收应付模块（往来管理，业财核心）
1. **应收账款**：客户应收、应收核销、对账单、逾期欠款统计
2. **应付账款**：供应商应付、应付核销、对账单
3. 预收账款、预付账款管理
4. 往来冲销、坏账处理

> 来源：销售出库产生应收；采购入库产生应付；收款/付款做核销。

### 📒 7、总账财务模块（财务核心）
1. **记账凭证管理**
    - 手工录入凭证；业务单据自动生成凭证
    - 凭证审核、反审核、凭证作废、红字冲销
2. 科目余额表：各科目本期发生、借方、贷方、期末余额
3. 明细账：每一笔科目流水明细
4. 日记账：银行、现金日记账
5. 期末处理：损益结转、期末结账、反结账
> ⚠️ 结账之后，当月业务单据不能修改，需要红字冲销

### 📊 8、财务报表模块
1. 资产负债表
2. 利润表（损益表）
3. 现金流量表（简易版）
4. 自定义报表：应收账龄、应付账龄、部门费用统计、项目利润统计

### 💳 9、费用报销模块
1. 费用申请单、报销单
2. 费用审核流程
3. 费用付款；自动生成费用凭证

### 📑10、固定资产模块（可选）
1. 固定资产卡片录入
2. 折旧计提，每月自动折旧生成凭证
3. 资产变更、资产报废

### 📥11、出纳模块
1. 银行流水导入（Excel）
2. 流水和收款/付款单对账匹配
3. 现金、银行日记账

### 📤12、报表&导出模块
- 全部单据、账簿支持Excel导出；数据打印
- 报表缓存，减轻MySQL压力

### 🔁13、对接外部模块（扩展）
- 对接CRM：客户、合同、回款数据同步
- 对接税务（发票导入）
- 消息通知：审核提醒、欠款预警

---

## 二、核心数据库表设计 MySQL8
> 统一规范：`id bigint primary`；`is_deleted tinyint DEFAULT 0` 逻辑删除；`created_at updated_at`；金额全部 `decimal(14,2)`，**禁止float**
> 核心原则：**业务单据表 和 财务凭证表分开，业务单据通过事件驱动生成凭证，不直接修改业务数据**

### 基础档案表
1. `sys_user` 用户
2. `sys_department` 部门
3. `sys_role`、`sys_role_permission` RBAC权限
4. `fin_account_subject` **会计科目表**（多级科目，辅助核算配置）
5. `fin_customer` 客户档案（和crm客户可做id关联）
6. `fin_supplier` 供应商档案
7. `fin_project` 项目档案
8. `fin_bank_account` 结算账户（银行、微信支付宝）
9. `fin_dict` 系统字典

### 业务单据表（采购、销售、库存）
1. `fin_purchase_order` 采购订单
2. `fin_purchase_in` 采购入库单 + `fin_purchase_in_item`入库明细
3. `fin_purchase_return` 采购退货单
4. `fin_pay_order` 采购付款单（付给供应商）

5. `fin_sale_order` 销售订单
6. `fin_sale_out` 销售出库单 + `fin_sale_out_item`出库明细
7. `fin_sale_return`销售退货单
8. `fin_receive_order`销售收款单（收客户钱）

6. `fin_stock_in/out/transfer/check` 库存相关单据

### 往来应收应付
1. `fin_receivable` 应收台账（客户应收）
2. `fin_payable` 应付台账（供应商应付）
3. `fin_receivable_writeoff` 应收核销记录
4. `fin_payable_writeoff` 应付核销记录

### 财务总账核心表
1. `fin_voucher` **凭证主表**（凭证号、凭证日期、摘要、凭证字、是否审核、是否结账）
2. `fin_voucher_item` **凭证明细表**（核心！每一条借贷分录，科目id、借方金额、贷方金额、辅助核算：客户id、供应商id、项目id、部门id）
> 一张凭证多条分录，借贷必须平衡；业务单据会生成多条分录到此表

3. `fin_subject_balance` **科目余额表**
> 存储每个科目每个会计期间：期初、借方发生、贷方发生、期末余额；**为报表加速，避免实时大表计算**

4. `fin_expense_bill` 费用报销单
5. `fin_fixed_asset` 固定资产卡片

### 系统辅助表
1. `fin_account_period` 会计期间表（记录每个月是否已经结账）
2. `sys_oper_log` 操作日志

> 重要字段：所有业务单据带上 `voucher_id`，记录该单据生成的凭证ID；单据作废时需要同步作废对应凭证。

**关键索引**
- voucher_id、subject_id、period、customer_id、supplier_id、project_id、is_deleted 建立索引
- 凭证表数据量会非常大，业务量大建议按会计期间做分表思路。

---

## 三、Laravel12 架构设计
### 分层架构
```
Controller → Request(表单验证) → Service业务层 → Model模型
```
1. **Controller**：接收请求，调用Service，返回响应
2. **FormRequest**：统一参数校验
3. **Service层【核心】**
    - 业务单据逻辑、审核逻辑、**业务单据生成凭证逻辑**
    - 事务在这里处理；一个业务操作必须放DB事务
4. **Model**：Laravel Model，关联关系、软删除、访问器修改器
5. **Repository（可选）**：封装复杂查询、报表查询；统一处理数据权限

### Laravel核心能力使用
1. **事件&监听 Event/Listener【业财一体化灵魂】**
> 不要在业务Service里面直接写生成凭证代码，解耦！
示例：
- `PurchaseInCreated`（采购入库审核完成事件）
- 监听器 `GenerateVoucherListener` 接收事件，自动生成记账凭证
好处：业务层只关心业务，财务凭证由监听异步生成；后续修改凭证规则只改监听器，不动采购销售代码。

2. **队列 Queue（Laravel Job）**
- 大量历史单据批量生成凭证丢队列
- 月末折旧计提、损益结转大运算丢队列
- 报表预计算缓存Job

3. **RBAC权限**：使用 `spatie/laravel-permission` 做角色、权限；自行扩展**数据权限**（Repository自动拼接where条件）

4. Redis用途
- 报表结果缓存（科目余额、资产负债表）
- 队列驱动
- 单据编号锁（防止重复生成凭证号、订单号）
- 会计期间锁，防止并发结账

### ⚠️ 必须使用数据库事务的场景
1. 业务单据审核 + 生成凭证（业务失败，凭证不能存在）
2. 凭证保存（多条借贷分录，必须借贷平衡）
3. 期末结账：更新科目余额、标记会计期间已结账
4. 收款核销应收、付款核销应付
5. 红字冲销作废凭证

### 业务关键规则（业财坑点）
1. **一旦会计期间结账，禁止修改当月业务单据；修改只能红字冲销生成新凭证**，不能直接改旧数据。
2. 金额全部 `decimal(14,2)`，PHP计算使用 `bc数学函数`，禁止浮点数运算。
3. 业务单据和凭证强关联：单据作废，对应的凭证必须作废。
4. 辅助核算：科目开启辅助核算，分录行带上客户/供应商/项目ID，用于往来账、项目利润统计。
5. 科目余额表：不要每次报表都聚合凭证明细；期末结账计算写入余额表，提升报表速度。
6. 凭证号：按月连续编号，同一个月份凭证号不能重复。
7. 软删除：财务数据严禁物理删除，全部逻辑删除，保证审计追溯。

## 四、和CRM系统对接方案
> 你前面CRM项目，可以做两套系统分离，通过接口同步：
1. CRM新增合同、回款记录 → 推送接口到业财系统生成销售收款单
2. 客户档案双向同步；财务系统读取CRM客户ID
3. 也可以共用一套mysql数据库，直接关联表 `crm_shturl.cc/o = fin_customer.crm_id`

## 五、部署架构
- Nginx + Laravel12-FPM
- MySQL8 主库；报表复杂查询可以读从库
- Redis：队列、缓存、分布式锁
- 存储：本地/OSS存储单据附件、Excel上传文件

## 六、可选扩展
1. 电子发票PDF上传识别
2. 银行对账单自动导入对账
3. 多账套（多公司）
4. 税务申报导出

---

如果你需要，我可以输出：
1. 核心建表SQL（mysql8，可直接导入）
2. Laravel事件+监听器示例（采购入库审核自动生成凭证示例代码）
3. 凭证借贷平衡校验代码示例
4. 科目余额表计算逻辑代码片段