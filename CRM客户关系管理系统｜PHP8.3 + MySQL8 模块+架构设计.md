# CRM客户关系管理系统｜PHP8.3 + MySQL8 模块+架构设计
> 技术栈：PHP8.3（Hyperf3.1 / ThinkPHP8均可）+ MySQL8 + Redis（缓存、登录令牌、限流）；适合中小企业CRM，客户、线索、跟进、合同、订单、回款、权限完整闭环。

## 一、核心业务模块清单
### 1. 系统权限模块（基础）
1. 用户管理：系统账号、员工状态、离职禁用、头像、部门岗位
2. 部门管理：组织架构，支持多级部门
3. 角色管理：RBAC角色，角色绑定权限
4. 权限管理：菜单权限、按钮权限、数据权限（**重点**：只能看自己/部门/全部客户）
5. 操作日志：谁、什么时间、做了什么、IP；登录日志

### 2. 线索模块（潜在客户，未成交）
- 线索录入：来源、联系人、电话、公司、需求、预算、跟进状态
- 线索分配：自动分配/手动分配给销售
- 线索转化：转为正式客户；无效线索归档
- 线索池：公海线索，回收、捞取

### 3. 客户模块（已转化客户）
- 客户档案：客户名称、行业、规模、联系人多个、电话、地址、客户等级、客户来源
- 联系人管理：一个客户多个联系人（主联系人、次要联系人）
- 客户公海：长期未跟进客户自动回收公海
- 客户标签：多标签，用于筛选
- 客户转移：客户移交给其他销售

### 4. 跟进记录模块（销售跟进）
> 核心：每次拜访、电话、微信沟通都记在这里
- 跟进类型：电话、上门、微信、会议
- 跟进内容、下次联系时间
- 关联客户/线索，关联对应销售
- 提醒：下次跟进提醒（Redis定时任务Hyperf定时消费）

### 5. 产品模块
- 产品分类
- 产品档案：名称、规格、售价、成本、库存、状态启用禁用
- 产品标签

### 6. 合同模块
- 合同录入：关联客户、签约销售、合同编号、签约日期、到期日期、总金额
- 合同附件上传
- 合同状态：待审核、已生效、已到期、作废
- 合同审批流（简易：审核通过/驳回）

### 7. 订单模块
- 订单关联客户、合同、产品，多行订单明细
- 订单金额、优惠、实付金额
- 订单状态：待确认、执行中、已完成、已取消

### 8. 回款（收款）模块【财务重点】
- 回款计划：合同预计回款时间、计划金额
- 实际回款记录：回款时间、金额、支付方式、凭证附件
- 欠款统计：哪些客户还有多少钱没收回
> 财务报表基于回款+合同统计营收

### 9. 工单/售后模块（可选）
客户售后问题，工单提交、指派处理人、处理进度、关闭工单

### 10. 统计报表模块
1. 销售统计：每个销售线索量、客户量、合同金额、回款金额
2. 客户统计：新增客户、流失客户、客户来源分布
3. 业绩报表：按月/季度业绩
4. 跟进统计：销售跟进频次

### 11. 系统配置模块
字典管理（下拉选项：客户来源、跟进类型、客户等级）、系统参数、短信/邮件配置

---

## 二、数据库核心表设计（MySQL8，精简核心表）
> 主键统一bigint，逻辑删除`is_deleted tinyint(1)`，创建时间`created_at`，更新时间`updated_at`

1. `sys_user` 系统用户表（员工账号）
2. `sys_department` 部门表
3. `sys_role` 角色表
4. `sys_role_permission` 角色权限关联
5. `sys_menu` 菜单权限表
6. `sys_oper_log` 操作日志

7. `crm_leads` 线索表（潜在客户）
8. `crm_customer` 正式客户表
9. `crm_customer_contact` 客户联系人表（一对多）
10. `crm_follow_record` 跟进记录表
11. `crm_tag` 标签表
12. `crm_tag_relation` 标签关联表（线索/客户打标签）

13. `crm_product` 产品表
14. `crm_product_category` 产品分类

15. `crm_contract` 合同主表
16. `crm_order` 订单主表
17. `crm_order_item` 订单产品明细

18. `crm_receivable_plan` 回款计划表
19. `crm_receivable_record` 实际回款记录表

20. `crm_work_order` 售后工单表

> 关键外键逻辑：所有业务表带上`owner_user_id`负责人ID，用于**数据权限过滤**；`create_user_id`创建人。

### 关键索引建议
- owner_user_id、customer_id、leads_id、create_time 建立索引；
- 手机号单独索引，用于查重；
- 逻辑删除条件统一带上 `is_deleted=0`

## 三、PHP后端架构设计（Hyperf3.1 微服务风格，也可适配ThinkPHP8）
### 分层架构
```
Controller 控制器 → Service业务层 → Repository数据库层 → Model模型
```
1. **Controller**：接收http请求，参数校验，调用Service，返回数据
2. **Service**：核心业务逻辑（线索分配、客户转移、合同创建、回款逻辑），事务在这里处理
3. **Repository**：封装查询、分页、条件过滤；统一处理数据权限过滤
4. **Model**：MySQL模型，关联关系（hasMany、belongsTo），软删除、时间自动转换

### 公共中间件
1. JWT登录鉴权中间件
2. **数据权限中间件（非常重要CRM核心）**
    根据当前登录用户角色，自动拼接查询条件：
    - 普通销售：只能看自己 `owner_user_id = 当前uid`
    - 部门主管：看本部门全部员工数据
    - 管理员：全部数据
3. 请求参数校验中间件
4. 操作日志中间件：自动记录增删改操作

### Redis使用场景
1. JWT登录token缓存
2. 客户/线索公海捞取防重复抢占
3. 跟进待提醒任务队列（Hyperf定时任务消费，推送站内信/短信提醒销售）
4. 报表统计缓存，减少mysql压力

### 定时任务（Hyperf crontab）
1. 定时回收长期未跟进客户到公海
2. 到期跟进提醒任务生成
3. 合同到期预警
4. 回款计划到期提醒

### 事务重点场景（必须加数据库事务）
- 线索转化客户（线索标记已转化，新增客户档案）
- 创建合同+生成回款计划
- 新增回款记录，更新合同已收金额

## 四、业务关键坑点（PHP+MySQL开发注意）
1. **数据权限**：不要每个service手写where条件，封装统一Repository，所有查询自动注入权限条件，避免漏写造成越权看别人客户。
2. 手机号查重：线索、客户联系人，防止重复录入同一个客户。
3. 软删除：全部业务表使用逻辑删除，不要物理删除，报表统计需要历史数据。
4. 金额字段：使用`decimal(12,2)`，**禁止float/double存金额**，防止精度丢失。
5. 公海抢占：多销售同时捞取同一个客户，Redis锁防止并发冲突。
6. 大报表不要直接count大表，必要时分页+缓存。

## 五、简单部署架构
> 你的服务器：阿里云ECS PHP8.3 + Mysql8 + Redis + Nginx
- 后端接口：Hyperf3.1 Swoole服务
- Mysql8主库，重要报表可以做只读查询分离
- Redis做缓存、消息队列
- 前端：Vue3前后端分离

## 六、可选扩展模块
1. 客户回访计划
2. 邮件批量发送、短信通知
3. 简单审批流
4. 客户导入导出Excel（PHPExcel/PhpSpreadsheet）
5. 客户重复合并（重复客户档案合并）

如果你需要，我可以输出：
1. 完整建表SQL（可直接导入MySQL8）
2. Hyperf模型示例代码（客户、线索、跟进）
3. 数据权限Repository示例代码