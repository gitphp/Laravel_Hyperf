       
# adc-finance 财务 业财一体化系统架构分析

## 一、整体架构概览

该项目是一个基于 **Hyperf 3.1** 框架构建的**财务会计管理微服务**，采用现代化的分层架构设计，支持高并发、分布式部署。

### 核心技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 语言 | PHP 8.1+ | 强类型支持，性能优异 |
| 框架 | Hyperf 3.1 | 协程框架，高性能微服务基础设施 |
| 运行时 | Swoole | 高性能异步网络通信引擎 |
| 数据库 | MySQL | 主数据存储 |
| 缓存 | Redis | 数据缓存、会话管理 |
| 消息队列 | RabbitMQ (AMQP) | 异步任务处理 |
| 服务发现 | Nacos | 服务注册与发现 |

### 服务端口配置

- **HTTP API**: `9611`
- **JSON-RPC**: `9602` (配置文件) / `9711` (文档)

---

## 二、项目目录结构

```
adc-finance/
├── app/
│   ├── Controller/V1/         # REST API 控制器（版本化）
│   │   ├── Accounts/          # 科目体系、凭证管理
│   │   ├── BankCompany/       # 银行账户、对账
│   │   ├── Settlement/        # 结算单、客户账单
│   │   ├── Reimbursement/     # 费用报销
│   │   ├── Invoice/           # 发票管理
│   │   ├── Assistant/         # 辅助配置（币种、汇率等）
│   │   ├── Report/            # 报表
│   │   ├── PeriodEnd/         # 期末处理
│   │   └── ...
│   ├── Model/                 # 数据模型（ORM）
│   ├── JsonRpc/Provider/      # JSON-RPC 服务提供者
│   ├── Amqp/                  # 消息队列（Producer/Consumer）
│   ├── Service/               # 业务逻辑层
│   ├── Resource/              # 资源层（DTO）
│   ├── Middleware/            # 中间件
│   ├── Listener/              # 事件监听器
│   └── Support/               # 工具类、支持组件
├── config/
│   └── autoload/              # 配置文件（数据库、服务、中间件等）
└── test/                      # 测试用例
```

---

## 三、核心业务模块

根据 `CLAUDE.md` 和代码结构，系统包含以下核心业务模块：

### 1. 凭证管理（Voucher）

| 实体 | 职责 |
|------|------|
| `Voucher` | 凭证主表 |
| `VoucherEntry` | 凭证分录 |
| `VoucherAttachment` | 凭证附件 |
| `VoucherStatusHistory` | 凭证状态变更历史 |

### 2. 科目体系（Accounting）

| 实体 | 职责 |
|------|------|
| `AccountSet` | 账套 |
| `AccountSubject` | 会计科目 |
| `AccountLedger` | 账簿 |
| `AccountCalendar` | 会计日历（年度） |
| `AccountPeriod` | 会计期间 |

### 3. 多币种（Currency）

| 实体 | 职责 |
|------|------|
| `Currency` | 币种定义 |
| `ExchangeRateTemplate` | 汇率模板 |
| `PeriodCurrency` | 期间币种 |

### 4. 银行管理（Bank）

| 实体 | 职责 |
|------|------|
| `BankCompanyAccount` | 银行账户 |
| `BankCompanyStatement` | 银行对账单 |
| `BankCompanyInternalTransfer` | 内部转账 |
| `BankCompanyBalanceAdjustment` | 余额调整 |

### 5. 结算管理（Settlement）

| 实体 | 职责 |
|------|------|
| `SettlementOrder` | 结算单 |
| `SettlementCustomerBill` | 客户账单 |
| `SettlementPaymentOrder` | 付款单 |
| `SettlementEntity` | 结算主体 |

### 6. 报销管理（Reimbursement）

| 实体 | 职责 |
|------|------|
| `Reimbursement` | 报销单 |
| `ReimbursementAttachment` | 报销附件 |

### 7. 业务配置（Business）

| 实体 | 职责 |
|------|------|
| `BusinessCate` | 业务类别 |
| `BusinessDetail` | 业务明细 |
| `MainCate` | 主类别 |

---

## 四、架构设计模式

### 1. 控制器层（Controller）

采用**版本化 API 设计**，所有控制器位于 `app/Controller/V1/` 目录下：

- 使用 **注解路由**（`#[GetMapping]`, `#[PostMapping]`），无需配置 `routes.php`
- 通过 `#[OrgPermission]` 注解实现**权限控制**
- 继承 `AbstractController` 获取通用依赖注入

**示例**（`AccountCalendarController.php`）：

```php
#[Controller('v1')]
class AccountCalendarController extends AbstractPeriodManagementController
{
    #[Inject]
    protected AccountCalendarService $service;

    #[OrgPermission(module: '期间管理', type: 'MENU', ...)]
    #[GetMapping(path: 'calendar')]
    public function list(AccountCalendarRequest $request): ResponseInterface
    {
        $data = $request->validated();
        $paginator = $this->service->calendarList($data);
        return ApiResponseHelper::success(AccountCalendarCollection::make($paginator));
    }
}
```

### 2. 资源层模式（Resource Layer）

遵循**双层资源模式**：

| 层级 | 用途 |
|------|------|
| `Resource/Domain/` | 内部 RPC 使用的原始数据结构 |
| `Resource/Application/V1/` | HTTP API 响应结构（带版本） |

### 3. 服务层（Service）

业务逻辑集中在 `Service` 层，通过依赖注入供控制器调用。

### 4. 消息队列（AMQP）

异步任务处理：

| Producer | Consumer | 用途 |
|----------|----------|------|
| `BankCompanyStatementImportProducer` | `BankCompanyStatementImportConsumer` | 银行对账单导入 |
| `SettlementOrderCreateProducer` | `SettlementOrderCreateConsumer` | 结算单创建 |
| `SettlementOrderUpdateProducer` | `SettlementOrderUpdateConsumer` | 结算单更新 |

### 5. JSON-RPC 通信

对外提供 RPC 服务，同时消费其他服务：

**对外服务**：
- `FinanceServiceProvider`
- `InvoiceServiceProvider`
- `SettlementOrderRpcProvider`
- `TenantFinanceBootstrapRpcProvider`

**消费服务**（通过 Nacos 发现）：
- `PublicService` - 文件、基础配置
- `HrService` - 员工/组织数据
- `UserService` - 用户/租户/权限
- `SystemService` - 系统配置
- `ApprovalService` - 审批服务

---

## 五、关键架构特性

### 1. 服务注册与发现

通过 **Nacos** 实现服务治理：

```php
// config/autoload/services.php
return [
    'enable' => [
        'discovery' => true,  // 服务发现
        'register' => true,   // 服务注册
    ],
    'drivers' => [
        'nacos' => [
            'url' => 'http://nacos.adc.com:8848',
            'group_name' => 'adc',
            'namespace_id' => '4de49d1b-804b-4c82-8818-985d68d88306',
        ],
    ],
];
```

### 2. 中间件机制

全局中间件链：

```php
// config/autoload/middlewares.php
return [
    'http' => [
        CorsMiddleware::class,           // 跨域处理
        TraceIdMiddleware::class,        // 链路追踪
        LocaleMiddleware::class,         // 国际化
        ValidationMiddleware::class,     // 请求验证
        PublicCustomerBillH5BypassOrgMiddleware::class, // H5 免权限
    ],
];
```

### 3. 数据库连接池

配置了连接池优化性能：

```php
// config/autoload/databases.php
'pool' => [
    'min_connections' => 1,
    'max_connections' => 10,
    'connect_timeout' => 10.0,
    'wait_timeout' => 3.0,
    'max_idle_time' => 60,
],
```

### 4. 模型 Traits

提供可复用的模型特性：

| Trait | 功能 |
|-------|------|
| `GeneratesAutoCode` | 自动生成业务编号 |
| `SoftDeletesWithDeletedBy` | 软删除并记录操作人 |

---

## 六、部署与运维

### 启动命令

```bash
composer start          # 启动服务
composer test           # 运行测试
composer cs-fix         # 修复代码风格
composer analyse        # PHPStan 静态分析
sh restart.sh           # 容器内重启服务
```

### 配置中心

支持通过 **Nacos Config Center** 进行配置管理（`config/autoload/config_center.php`）。

---

## 七、总结

该项目是一个**成熟的企业级财务会计管理微服务**，具备以下特点：

1. **技术先进性**：基于 Hyperf 协程框架，支持高并发
2. **架构完整性**：完整的分层设计（Controller → Service → Model）
3. **服务化能力**：支持 REST API + JSON-RPC 双协议
4. **异步处理**：通过 AMQP 实现异步任务解耦
5. **可维护性**：清晰的目录结构、版本化 API、代码规范工具
6. **扩展性**：服务发现、配置中心支持分布式部署