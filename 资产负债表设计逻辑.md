
          
根据代码分析，我来详细说明资产负债表中**资产**和**负债与所有者权益**的数据来源及取数逻辑。

---

## 一、核心数据来源表

### 1. `account_balance` — 科目余额主表（核心数据源）

| 字段 | 类型 | 说明 |
|------|------|------|
| `account_id` | int | 账套ID |
| `account_subject_id` | int | 科目ID |
| `period_id` | int | 期间ID |
| `balance_type` | string | 余额类型（approved=已审核，inclusive=包含未审核） |
| `ending_dr_func` | decimal(6) | **期末借方余额-本位币** |
| `ending_cr_func` | decimal(6) | **期末贷方余额-本位币** |
| `beginning_dr_func` | decimal(6) | **期初借方余额-本位币** |
| `beginning_cr_func` | decimal(6) | **期初贷方余额-本位币** |

**核心取数逻辑**（`BalanceSheetReportService::loadBalanceMap` 方法）：

```php
$rows = AccountBalance::query()
    ->where('account_id', $accountId)
    ->where('balance_type', BalanceType::APPROVED)  // 仅取已审核账
    ->whereIn('period_id', $periodIds)
    ->whereIn('account_subject_id', $subjectIds)
    ->selectRaw(
        'period_id, account_subject_id,'
        . ' SUM(ending_dr_func) AS e_dr, SUM(ending_cr_func) AS e_cr,'
        . ' SUM(beginning_dr_func) AS b_dr, SUM(beginning_cr_func) AS b_cr'
    )
    ->groupBy('period_id', 'account_subject_id')
    ->get();
```

### 2. `account_subject` — 会计科目表（科目关系与方向）

| 字段 | 用途 |
|------|------|
| `full_segment_code` | 科目全编码（如 1001、1002.01） |
| `parent_id` | 父科目ID（构建科目树） |
| `loan_direction` | 借贷方向（1=借方，2=贷方，0=无方向） |

### 3. `account_balance_sheet_template_item` — 报表行项目表（公式定义）

存储资产负债表每一行的取数公式，核心字段：
- `parsed_formula_beginning_balance`：年初余额公式（已解析为AST）
- `parsed_formula_ending_balance`：期末余额公式（已解析为AST）
- `section_type`：区块类型（1=资产，2=负债与所有者权益）

---

## 二、取数逻辑详解

### 1. 整体流程（`BalanceSheetReportService::balanceSheetTable`）

```
用户请求 → 获取账套/期间 → 加载报表模板 → 解析公式AST → 构建科目余额Resolver → 
计算期末余额 → 计算年初余额 → 平衡校验 → 返回结果
```

### 2. 科目余额计算（`calculateSubjectBalance` 方法）

```php
private function calculateSubjectBalance(array $balanceMap, array $ids, int $pid, bool $useBeginning, int $loanDirection): float
{
    $dr = 0.0;
    $cr = 0.0;
    foreach ($ids as $sid) {
        $row = $balanceMap[$pid][$sid] ?? null;
        if ($row === null) continue;
        $dr += $useBeginning ? $row['b_dr'] : $row['e_dr'];  // 期初取b_dr，期末取e_dr
        $cr += $useBeginning ? $row['b_cr'] : $row['e_cr'];  // 期初取b_cr，期末取e_cr
    }
    return $this->netByDirection($dr, $cr, $loanDirection);  // 按借贷方向取净额
}
```

### 3. 净额计算规则（`netByDirection` 方法）

```php
// 贷方科目取「贷 - 借」，借方/无方向取「借 - 贷」
private function netByDirection(float $dr, float $cr, int $loanDirection): float
{
    return LoanDirectionI18n::isCredit($loanDirection) ? $cr - $dr : $dr - $cr;
}
```

**举个例子**：
- **资产类科目（借方科目）**：现金(1001) 期末借方余额1000，贷方余额0 → 净额 = 1000 - 0 = 1000
- **负债类科目（贷方科目）**：应付账款(2202) 期末借方余额0，贷方余额500 → 净额 = 500 - 0 = 500

### 4. 科目汇总逻辑（`buildSubjectDescendantMap` 方法）

资产负债表取数时，**科目代码会自动汇总自身及所有子科目**：

```
1001 现金
├── 1001.01 人民币现金
└── 1001.02 美元现金

取数公式 {#1001#[*.*]@[B]} 会汇总：1001 + 1001.01 + 1001.02 的余额
```

---

## 三、资产 vs 负债与所有者权益 — 取数差异

### 资产区块（section_type = 1）

- **取数特点**：资产类科目均为**借方科目**，余额为正数表示资产增加
- **典型科目**：1001(现金)、1002(银行存款)、1122(应收账款)、1601(固定资产)等
- **计算公式**：`借 - 贷`（资产余额在借方）

### 负债与所有者权益区块（section_type = 2）

- **取数特点**：
  - **负债类**：贷方科目，余额为正数表示负债增加（如 2001 短期借款、2202 应付账款）
  - **所有者权益类**：贷方科目，余额为正数表示权益增加（如 4001 实收资本、4103 本年利润）
- **计算公式**：`贷 - 借`（负债和权益余额在贷方）

---

## 四、平衡校验机制

资产负债表必须满足会计恒等式：

```
资产总计 = 负债总计 + 所有者权益总计
```

校验逻辑（`buildBalanceCheck` 方法）：

```php
$endAsset = $endingByLine[$assetTotalLine];      // 资产期末总计
$endLiab = $endingByLine[$liabEquityTotalLine];  // 负债与权益期末总计
$endBalanced = abs($endAsset - $endLiab) < 0.005;  // 误差小于0.005视为平衡
```

---

## 五、取数公式示例

报表行项目中存储的公式结构：

| 行项目 | 公式示例 | 说明 |
|--------|----------|------|
| 货币资金 | `{#1001#[*.*]@[B]} + {#1002#[*.*]@[B]}` | 现金 + 银行存款期末余额 |
| 应收账款 | `{#1122#[*.*]@[B]}` | 应收账款期末余额 |
| 短期借款 | `{#2001#[*.*]@[B]}` | 短期借款期末余额 |
| 实收资本 | `{#4001#[*.*]@[B]}` | 实收资本期末余额 |
| 资产总计 | `{1} + {2} + {3} + ...` | 汇总资产各子项目 |

公式解析后生成的 AST 结构：

```php
// {#1001#[2025.04]@[B]}
[
    'type' => 'subject',
    'subject_code' => '1001',
    'period' => [
        'type' => 'single',
        'year' => 2025,
        'period' => 4
    ],
    'data_type' => 'B'  // B = 期末余额
]
```

---

## 六、总结

| 维度 | 资产区块 | 负债与所有者权益区块 |
|------|----------|---------------------|
| **section_type** | 1 | 2 |
| **数据源表** | `account_balance` | `account_balance` |
| **取数字段** | `ending_dr_func` / `ending_cr_func` | `ending_dr_func` / `ending_cr_func` |
| **净额方向** | 借 - 贷（借方科目） | 贷 - 借（贷方科目） |
| **科目类型** | 资产类（1开头） | 负债类（2开头）、权益类（4开头） |
| **校验方式** | 与负债+权益总计对比 | 与资产总计对比 |

**核心原理**：资产负债表通过公式解析器将行项目公式转换为科目取数指令，从 `account_balance` 表批量加载科目余额，按科目借贷方向计算净额，最后验证会计恒等式是否成立。